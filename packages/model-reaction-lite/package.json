{
  "name": "model-reaction-lite",
  "version": "0.1.0",
  "description": "Synchronous, minimal subset of model-reaction. No async validators, no debounce, no settled().",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "jest --config ./jest.config.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json"
  },
  "keywords": [
    "model",
    "validation",
    "reaction",
    "sync",
    "lite",
    "typescript"
  ],
  "license": "ISC",
  "private": true
}
